# ============================================================
#  GlobeNews — Python dependencies
#  Install with:  pip install -r requirements.txt
# ============================================================

# ── Web framework ─────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── HTTP client ───────────────────────────────────────────
# Pinned to avoid conflict with gnews 0.3.6 which requires 2.26.0.
# We use requests directly (not via gnews), so 2.28+ is fine.
requests>=2.28.0,<3.0.0

# ── Geocoding (free, no API key) ─────────────────────────
geopy>=2.4.1

# ── NLP / Named Entity Recognition (location extraction) ──
# spaCy and its lightweight English model for NER-based
# location extraction from article headlines.
# After pip install, run:  python -m spacy download en_core_web_sm
spacy>=3.7.0

# ── Environment variable management ──────────────────────
# Pinned to avoid conflict with gnews 0.3.6 which requires ~=0.19.0.
# We load .env manually so any modern version works fine.
python-dotenv>=0.19.0,<2.0.0

# ── Optional: faster JSON / async if you extend the app ──
# uvicorn>=0.29.0
# httpx>=0.27.0